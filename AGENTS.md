# odsbox-jaquel-mcp

This repository contains the code of an FastMCP agent that can be used to query
the `odsbox`python package to access ASAM ODS servers. Its setup to use modern Python tools.

* uv
* ruff
* mypy
* pytest
